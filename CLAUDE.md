# MEGURU 記事量産オートメーション

このリポジトリは、スケジュール実行（Routine）によって定期的に記事を追加していく
運用になっています。各実行は毎回まっさらなセッションとして起動され、会話の記憶を
持たないため、**必ず `content-plan.md` を読んでから作業してください。**

## 実行時の手順

1. `content-plan.md` を開き、`[ ]`（未完了）の最初のフェーズを見つける
2. そのフェーズの内容を実行する（新規記事作成 or 既存記事のブラッシュアップ）
3. 完了したら該当フェーズを `[x]` に変更し、次に実行すべきフェーズを追記する
   （記事作成フェーズの後には、必ずそのブラッシュアップフェーズを追加する）
4. `git add` → `git commit` → `git push origin claude/affectionate-cerf-03sxjp` で確定する

## 記事のルール

- 新しい記事は `templates/article-template.html` をベースに `articles/` へ追加し、
  `index.html` の `.article-grid` にカードを追加する（`README.md` の手順を参照）
- ジャンルはオールジャンル（テクノロジー / 旅 / グルメ / ライフスタイル / 健康 の
  既存カテゴリーを基本に、必要なら新ジャンルを追加してよい）
- 「アフィリエイト記事」は商品比較・選び方ガイドなど購買検討層向けの記事を指す。
  実在しない商品名・価格・実在しないアフィリエイトリンクは創作しないこと。
  選び方の軸や特徴の解説など、事実確認を要さない一般的な内容にとどめる
- 「集客記事」はアフィリエイト記事へ読者を誘導する、関連するお役立ち情報記事。
  対応するアフィリエイト記事と相互リンクさせる
- ブラッシュアップでは、文字数を増やす・見出しを追加する・具体例を増やす・
  関連記事リンクを増やすなど、記事の質と網羅性を上げる

## スケジュールについて

このRoutineは本来「9:20〜18:00の間、20分おきに実行」を意図して作られましたが、
このプラットフォームのトリガーは最短でも1時間間隔でしか実行できません。
そのため実際の発火間隔とは無関係に、上記の手順で「毎回1フェーズずつ」進める設計に
しています。頻度に関わらずこのファイルの手順に従えば、進行が破綻しません。
